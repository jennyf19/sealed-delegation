# Capacity check

Citation: `synthetic-load-test#capacity-check`

The planned test will open 24 concurrent sessions on the target host. No measured or configured
single-flight or session ceiling is included.
