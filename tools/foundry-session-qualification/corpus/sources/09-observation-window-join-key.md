# Experiment window join

Citation: `synthetic-experiment#window-join`

The release table contains release IDs and dates. The outcome table contains rollback events and
window labels, but it does not contain a release identifier shared with the release table.
