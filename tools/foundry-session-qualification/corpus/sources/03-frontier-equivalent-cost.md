# Routing cost comparison

Citation: `synthetic-routing#cost-comparison`

The workload used 18,200 input tokens and 1,140 output tokens. The comparison must use the
frontier model price that was effective on the measured date. The pricing schedule is not part of
the evidence package.
